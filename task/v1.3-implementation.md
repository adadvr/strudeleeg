# Tarea — functionalityv1.3.md (P0→P4 completo, por fases)

Cada fase: implementar → `swift test` (MiniEngine) + `swift build` → actualizar COMPATIBILITY.md → commit.
Clean-room desde docs públicas. RNG con seed. Modelo: sonnet.

## P0 — Validador de patrones  ← EN CURSO
- [ ] API `CodeParser.validate(_ code:) -> [Diagnostic]` (o tipo público en MiniEngine).
      Cada `Diagnostic`: `kind` (unsupported | arbitraryJS | ok-info), `token`/texto,
      `range` (posición en el código), `suggestion?`.
- [ ] Reutilizar `knownMethods` + `friendlyUnknown`; detectar métodos en la cadena
      que no estén soportados. Detectar JS arbitrario (const/=>/register/{}) heurístico.
- [ ] Nunca crashea; corre antes de reproducir.
- [ ] UI: mostrar diagnósticos legibles en el panel Mini Engine (y JUCE).
- [ ] Tests: patrón soportado → 0 diag; `pickOut(...)` → unsupported con sugerencia.
- [ ] COMPATIBILITY.md: sección "Validador".

## P1 — Soundfonts GM
- [ ] Cargar bancos soundfont del set pre-convertido de Strudel desde servidor remoto
      (reusar SampleBankManager). `s("gm_epiano1")`, `:n` variación.
- [ ] Mapeo nota→sample más cercano + repitch.
- [ ] Tests + COMPATIBILITY.md.

## P2 — Estructura de canción
- [ ] `pick(idxPat, [pat0, pat1, ...])`, `pickOut`, `pickRestart`.
- [ ] `layer(f1, f2, ...)` (transformaciones en paralelo, apila).
- [ ] Patrones etiquetados `nombre:` (equiv. `$:` con etiqueta).
- [ ] Tests (oracle) + COMPATIBILITY.md.

## P3 — Expresión y timing
- [ ] `clip(x)` (staccato↔legato, recorta whole).
- [ ] `late(x)` / `early(x)` (rotR/rotL fraccional).
- [ ] `transpose(n)` (semitonos), `velocity(x)`.
- [ ] Mini-notación `a/4` (slow dentro del string) — MiniNotationCore.
- [ ] Campos nuevos en ScheduledEvent (velocity, clip) + aplicar en scheduler(s).
- [ ] Tests + COMPATIBILITY.md.

## P4 — Acordes
- [ ] `chord("Am")`, `voicing()`, `anchor("g5")`. Requiere acordes con coma OK.
- [ ] Tabla de acordes (maj, min, 7, maj7, m7, dim, sus4, …) desde teoría pública.
- [ ] Tests + COMPATIBILITY.md.

## Al terminar todo
- [ ] Composición de referencia 3+ min con GM + 4 secciones vía pick + groove.
- [ ] Borrar este archivo.
